"""HTTP layer (aiohttp routes).

This package contains plain aiohttp endpoint handlers used by the webhook runtime.
They are intentionally kept separate from Telegram handlers to keep concerns clear.
"""
